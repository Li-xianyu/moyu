# Third-party vendor assets

This directory contains pinned third-party browser assets that are served locally
to avoid runtime CDN dependency and improve PWA/PageSpeed load stability.

These files are not MOYU application source code. When upgrading a dependency,
replace the matching versioned subdirectory and update the references in the app.
